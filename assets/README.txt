Put your own images here:

  bg.jpg       -> full-page background photo (a wide render works best, 1920x1080+)
  avatar.jpg   -> profile picture (square)
  work-1.jpg … -> portfolio thumbnails (square)

If bg.jpg is missing the site falls back to the built-in gradient background,
so nothing breaks while you are still preparing the images.
